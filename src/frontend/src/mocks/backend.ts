import type { backendInterface } from "../backend";
import {
  AttendanceStatus,
  ClassStatus,
  FeeStatus,
  FeeType,
  MaterialType,
  UserRole,
} from "../backend";

const now = BigInt(Date.now()) * BigInt(1_000_000);

export const mockBackend: backendInterface = {
  assignCallerUserRole: async () => undefined,
  cancelSession: async () => undefined,
  createBatch: async (args) => ({
    id: BigInt(1),
    courses: [],
    name: args.name,
    createdAt: now,
    year: args.year,
  }),
  createCourse: async (args) => ({
    id: BigInt(1),
    name: args.name,
    createdAt: now,
    description: args.description,
    isActive: true,
    teacherId: args.teacherId,
    batch: args.batch,
    schedule: args.schedule,
  }),
  createFee: async (args) => ({
    id: BigInt(1),
    status: FeeStatus.unpaid,
    studentId: args.studentId,
    createdAt: now,
    feeType: args.feeType,
    dueDate: args.dueDate,
    notes: args.notes,
    amount: args.amount,
  }),
  createSession: async (args) => ({
    id: BigInt(1),
    status: ClassStatus.upcoming,
    topic: args.topic,
    duration: args.duration,
    date: args.date,
    createdAt: now,
    time: args.time,
    teacherId: BigInt(1),
    courseId: args.courseId,
  }),
  createStudent: async (args) => ({
    id: BigInt(1),
    principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
    name: args.name,
    joiningDate: args.joiningDate,
    isActive: true,
    email: args.email,
    batch: args.batch,
    enrolledCourses: [],
  }),
  createTeacher: async (principal, args) => ({
    id: BigInt(1),
    principal,
    subjects: args.subjects,
    name: args.name,
    isActive: true,
    email: args.email,
    joinedDate: now,
    qualification: args.qualification,
  }),
  deleteMaterial: async () => undefined,
  deleteStudent: async () => undefined,
  deleteTeacher: async () => undefined,
  enrollStudentInCourse: async () => undefined,
  getAttendanceRate: async () => BigInt(87),
  getBatch: async () => ({
    id: BigInt(1),
    courses: [BigInt(1), BigInt(2)],
    name: "Batch 2024-A",
    createdAt: now,
    year: BigInt(2024),
  }),
  getCallerUserRole: async () => UserRole.admin,
  getCourse: async () => ({
    id: BigInt(1),
    name: "Mathematics Advanced",
    createdAt: now,
    description: "Advanced mathematics course",
    isActive: true,
    teacherId: BigInt(1),
    batch: BigInt(1),
    schedule: "Mon/Wed/Fri 10:00 AM",
  }),
  getDashboardAnalytics: async () => ({
    averageAttendance: BigInt(87),
    feeCollectionRate: BigInt(92),
    activeStudents: BigInt(142),
    totalStudents: BigInt(158),
    totalBatches: BigInt(6),
    totalMaterials: BigInt(34),
    totalTeachers: BigInt(12),
    activeCourses: BigInt(18),
  }),
  getFee: async () => ({
    id: BigInt(1),
    status: FeeStatus.paid,
    studentId: BigInt(1),
    createdAt: now,
    feeType: FeeType.monthly,
    dueDate: now,
    paidDate: now,
    notes: "Monthly fee",
    amount: BigInt(5000),
  }),
  getFeeCollectionRate: async () => BigInt(92),
  getMaterial: async () => null,
  getMyStudentProfile: async () => ({
    id: BigInt(1),
    principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
    name: "Arjun Sharma",
    joiningDate: now,
    isActive: true,
    email: "arjun.sharma@student.edutrack.com",
    batch: BigInt(1),
    enrolledCourses: [BigInt(1), BigInt(2)],
  }),
  getMyTeacherProfile: async () => ({
    id: BigInt(1),
    principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
    subjects: ["Mathematics", "Physics"],
    name: "Dr. Priya Patel",
    isActive: true,
    email: "priya.patel@teacher.edutrack.com",
    joinedDate: now,
    qualification: "PhD Mathematics",
  }),
  getPerformanceSummary: async () => ({
    marks: [BigInt(1), BigInt(2), BigInt(3)],
    studentId: BigInt(1),
    totalExams: BigInt(5),
    averagePercentage: BigInt(78),
    courseId: BigInt(1),
  }),
  getSession: async () => ({
    id: BigInt(1),
    status: ClassStatus.upcoming,
    topic: "Quadratic Equations",
    duration: BigInt(60),
    date: now,
    createdAt: now,
    time: "10:00 AM",
    teacherId: BigInt(1),
    courseId: BigInt(1),
  }),
  getStudent: async () => ({
    id: BigInt(1),
    principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
    name: "Arjun Sharma",
    joiningDate: now,
    isActive: true,
    email: "arjun.sharma@student.edutrack.com",
    batch: BigInt(1),
    enrolledCourses: [BigInt(1), BigInt(2)],
  }),
  getTeacher: async () => ({
    id: BigInt(1),
    principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
    subjects: ["Mathematics", "Physics"],
    name: "Dr. Priya Patel",
    isActive: true,
    email: "priya.patel@teacher.edutrack.com",
    joinedDate: now,
    qualification: "PhD Mathematics",
  }),
  isCallerAdmin: async () => true,
  listAttendanceByCourse: async () => [
    {
      id: BigInt(1),
      status: AttendanceStatus.present,
      studentId: BigInt(1),
      date: now,
      markedBy: BigInt(1),
      notes: "",
      courseId: BigInt(1),
    },
    {
      id: BigInt(2),
      status: AttendanceStatus.absent,
      studentId: BigInt(2),
      date: now,
      markedBy: BigInt(1),
      notes: "Sick leave",
      courseId: BigInt(1),
    },
  ],
  listAttendanceByDate: async () => [],
  listAttendanceByStudent: async () => [
    {
      id: BigInt(1),
      status: AttendanceStatus.present,
      studentId: BigInt(1),
      date: now,
      markedBy: BigInt(1),
      notes: "",
      courseId: BigInt(1),
    },
  ],
  listBatches: async () => [
    {
      id: BigInt(1),
      courses: [BigInt(1), BigInt(2)],
      name: "Batch 2024-A",
      createdAt: now,
      year: BigInt(2024),
    },
    {
      id: BigInt(2),
      courses: [BigInt(3)],
      name: "Batch 2024-B",
      createdAt: now,
      year: BigInt(2024),
    },
  ],
  listCourses: async () => [
    {
      id: BigInt(1),
      name: "Mathematics Advanced",
      createdAt: now,
      description: "Advanced mathematics for competitive exams",
      isActive: true,
      teacherId: BigInt(1),
      batch: BigInt(1),
      schedule: "Mon/Wed/Fri 10:00 AM",
    },
    {
      id: BigInt(2),
      name: "Physics Fundamentals",
      createdAt: now,
      description: "Core physics principles and applications",
      isActive: true,
      teacherId: BigInt(2),
      batch: BigInt(1),
      schedule: "Tue/Thu 2:00 PM",
    },
    {
      id: BigInt(3),
      name: "Chemistry Organic",
      createdAt: now,
      description: "Organic chemistry for board exams",
      isActive: true,
      teacherId: BigInt(3),
      batch: BigInt(2),
      schedule: "Mon/Wed 4:00 PM",
    },
  ],
  listCoursesByBatch: async () => [],
  listCoursesByTeacher: async () => [],
  listFees: async () => [
    {
      id: BigInt(1),
      status: FeeStatus.paid,
      studentId: BigInt(1),
      createdAt: now,
      feeType: FeeType.monthly,
      dueDate: now,
      paidDate: now,
      notes: "Monthly tuition fee",
      amount: BigInt(5000),
    },
    {
      id: BigInt(2),
      status: FeeStatus.unpaid,
      studentId: BigInt(2),
      createdAt: now,
      feeType: FeeType.monthly,
      dueDate: now,
      notes: "Monthly tuition fee",
      amount: BigInt(5000),
    },
    {
      id: BigInt(3),
      status: FeeStatus.partial,
      studentId: BigInt(3),
      createdAt: now,
      feeType: FeeType.quarterly,
      dueDate: now,
      notes: "Quarterly fee - partial payment",
      amount: BigInt(12000),
    },
  ],
  listFeesByStudent: async () => [],
  listMarksByCourse: async () => [],
  listMarksByStudent: async () => [
    {
      id: BigInt(1),
      marks: BigInt(85),
      studentId: BigInt(1),
      date: now,
      recordedBy: BigInt(1),
      maxMarks: BigInt(100),
      examType: "Unit Test 1",
      courseId: BigInt(1),
      remarks: "Good performance",
    },
    {
      id: BigInt(2),
      marks: BigInt(72),
      studentId: BigInt(1),
      date: now,
      recordedBy: BigInt(1),
      maxMarks: BigInt(100),
      examType: "Mid Term",
      courseId: BigInt(2),
      remarks: "Needs improvement in thermodynamics",
    },
  ],
  listMaterials: async () => [],
  listMaterialsByCourse: async () => [],
  listSessionsByCourse: async () => [],
  listSessionsByTeacher: async () => [
    {
      id: BigInt(1),
      status: ClassStatus.upcoming,
      topic: "Quadratic Equations",
      duration: BigInt(60),
      date: now,
      createdAt: now,
      time: "10:00 AM",
      teacherId: BigInt(1),
      courseId: BigInt(1),
    },
    {
      id: BigInt(2),
      status: ClassStatus.completed,
      topic: "Linear Algebra Basics",
      duration: BigInt(90),
      date: now,
      createdAt: now,
      time: "10:00 AM",
      teacherId: BigInt(1),
      courseId: BigInt(1),
    },
  ],
  listStudents: async () => [
    {
      id: BigInt(1),
      principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
      name: "Arjun Sharma",
      joiningDate: now,
      isActive: true,
      email: "arjun.sharma@student.edutrack.com",
      batch: BigInt(1),
      enrolledCourses: [BigInt(1), BigInt(2)],
    },
    {
      id: BigInt(2),
      principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
      name: "Priya Nair",
      joiningDate: now,
      isActive: true,
      email: "priya.nair@student.edutrack.com",
      batch: BigInt(1),
      enrolledCourses: [BigInt(1)],
    },
    {
      id: BigInt(3),
      principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
      name: "Rahul Verma",
      joiningDate: now,
      isActive: false,
      email: "rahul.verma@student.edutrack.com",
      batch: BigInt(2),
      enrolledCourses: [BigInt(3)],
    },
  ],
  listTeachers: async () => [
    {
      id: BigInt(1),
      principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
      subjects: ["Mathematics", "Physics"],
      name: "Dr. Priya Patel",
      isActive: true,
      email: "priya.patel@teacher.edutrack.com",
      joinedDate: now,
      qualification: "PhD Mathematics",
    },
    {
      id: BigInt(2),
      principal: { _arr: new Uint8Array(), _isPrincipal: true } as any,
      subjects: ["Chemistry"],
      name: "Mr. Rajan Kumar",
      isActive: true,
      email: "rajan.kumar@teacher.edutrack.com",
      joinedDate: now,
      qualification: "M.Sc Chemistry",
    },
  ],
  recordAttendance: async (args) => ({
    id: BigInt(1),
    status: args.status,
    studentId: args.studentId,
    date: args.date,
    markedBy: BigInt(1),
    notes: args.notes,
    courseId: args.courseId,
  }),
  recordMark: async (args) => ({
    id: BigInt(1),
    marks: args.marks,
    studentId: args.studentId,
    date: args.date,
    recordedBy: BigInt(1),
    maxMarks: args.maxMarks,
    examType: args.examType,
    courseId: args.courseId,
    remarks: args.remarks,
  }),
  seedDemoData: async () => "Demo data seeded successfully",
  unenrollStudentFromCourse: async () => undefined,
  updateCourse: async () => undefined,
  updateFee: async () => undefined,
  updateSession: async () => undefined,
  updateStudent: async () => undefined,
  updateTeacher: async () => undefined,
  uploadMaterial: async (args) => ({
    id: BigInt(1),
    title: args.title,
    fileBlob: args.fileBlob,
    description: args.description,
    fileName: args.fileName,
    courseId: args.courseId,
    uploadDate: now,
    materialType: args.materialType,
    uploadedBy: BigInt(1),
  }),
  _immutableObjectStorageBlobsAreLive: async () => [],
  _immutableObjectStorageBlobsToDelete: async () => [],
  _immutableObjectStorageConfirmBlobDeletion: async () => undefined,
  _immutableObjectStorageCreateCertificate: async () => ({
    method: "GET",
    blob_hash: "",
  }),
  _immutableObjectStorageRefillCashier: async () => ({}),
  _immutableObjectStorageUpdateGatewayPrincipals: async () => undefined,
  _initializeAccessControl: async () => undefined,
};
